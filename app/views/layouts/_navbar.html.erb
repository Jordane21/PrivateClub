<nav class="navbar navbar-default">
    <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Private Club</a>
     </div>
    <ul class="nav navbar-nav">
      <li class="active"><a href="#">Home</a></li>
        <% if user_signed_in? %>
          <%= link_to "Page Club", page_club_show_path %>
        <% end %>
      </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path %>
            <%= link_to "Sign Up", new_user_registration_path %>
          <% end %>
    
    </div>
    </nav>

    <li><a href="<%= link_to "Sign In", new_user_session_path %>"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
    <li><a href="<%= user_session_path%>"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>


